public class Method {
    
    /*
     public int add(int x, int y){
        return x + y;
    }

    public String demo(String[] textArray, String count) {
        int counter = 0;
        for( String s : textArray){

            if (!s.isEmpty() && (s.startsWith(count.toLowerCase()) || (s.startsWith(count.toUpperCase())))) {
                counter++;
            }
        }
        return "Jag hittade " + counter + " ord som började med " + count;
    }
     */
    
    
}
